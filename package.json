{
  "name": "@tiberjs/runner",
  "version": "0.1.1",
  "description": "Transport-independent execution runtime: immutable context, structured concurrency, dependency injection, and application lifecycle.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rspack build && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --deny-warnings .",
    "lint:fix": "oxlint --fix --deny-warnings .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "check": "pnpm lint && pnpm format:check && pnpm typecheck",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@12.3.4",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tiberjs/runner.git"
  },
  "author": "miinhho",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@rspack/cli": "2.2.3",
    "@rspack/core": "2.2.3",
    "@swc/helpers": "0.5.23",
    "@types/node": "^24",
    "oxfmt": "0.67.0",
    "oxlint": "1.82.0",
    "typescript": "^5",
    "vitest": "^5.0.0"
  }
}
